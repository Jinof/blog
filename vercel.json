{
  "installCommand": "curl https://sh.rustup.rs -sSf | sh -s -- -y --profile minimal --target wasm32-unknown-unknown && . \"$HOME/.cargo/env\" && cargo install wasm-bindgen-cli --locked",
  "buildCommand": ". \"$HOME/.cargo/env\" && cargo run --release -- build",
  "outputDirectory": "public"
}
